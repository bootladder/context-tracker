this is the msgqueue server daemon
that waits for collectors to send JSON data to it,
then it dumps to a file.

the files will be per collecotor, identified somehow by payloads or msgqueue metadata


