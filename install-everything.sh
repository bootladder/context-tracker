#!/bin/bash

# build the local webapp


# install all systemd services

# for each collector adn the data receiver and local webapp
# copy the daemon to /usr/local/bin
# copy systemd.unit scriptt to /etc
# stop enable start systemd service






